# Shared dev tooling for the VibeBite monorepo.
# Per-service runtime dependencies live in each services/*/pyproject.toml.
# Install with: pip install -r requirements-dev.txt
#
# Versions pinned to recent stable releases; bump intentionally.

ruff==0.7.4
black==24.10.0
mypy==1.13.0
pytest==8.3.3
pytest-cov==6.0.0
pre-commit==4.0.1
